package Factory.Vehicles;

import Factory.Vehicle;

public class Cars extends Vehicle {
    public Cars(boolean up, boolean down, boolean left, boolean right) {
        super(up, down, left, right);
    }
}