package Factory.Vehicles;

import Factory.Vehicle;

public class Bikes extends Vehicle {
    public Bikes(boolean up, boolean down, boolean left, boolean right) {
        super(up, down, left, right);
    }
}